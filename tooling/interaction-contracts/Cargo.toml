[package]
name = "minutes-interaction-contracts"
version = "0.1.0"
edition = "2021"
publish = false

# Test the SAME production files without the audio/desktop dependency graph.
[workspace]

[lib]
path = "../../crates/core/src/interaction/mod.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
