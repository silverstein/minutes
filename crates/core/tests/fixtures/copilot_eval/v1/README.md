# Copilot eval corpus v1

Every transcript in this directory is synthetic. No private meeting content,
customer data, or copied conversation text is included.

The corpus covers long-turn partial synthesis, a semantic partial reversal,
overlapping opportunities, duplicate suppression, and an extended interval in
which silence is the correct coaching behavior. JSON files are embedded in the
binary for the default suite and can also be replaced with `--fixtures DIR`.
